model User {
  id        String    @id @default(uuid())
  email     String    @unique
  name      String?
  password  String
  boards    Board[]
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
  deletedAt DateTime?
}
